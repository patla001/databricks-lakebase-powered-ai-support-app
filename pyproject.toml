[project]
name = "databricks-lakebase-powered-ai-support-app"
version = "0.1.0"
description = "Support ticket app on Databricks Apps, backed by Lakebase Postgres"
readme = "README.md"
requires-python = ">=3.11"
# Dependencies live in requirements.txt - that is the file Databricks Apps
# installs from, so keeping a second list here would only invite drift.
dependencies = []
